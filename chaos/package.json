{
  "name": "bosphor-chaos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "On-demand chaos / failure-injection harness for the Bosphor cross-chain system",
  "scripts": {
    "chaos": "tsx src/main.ts",
    "test": "tsx --test src/**/*.spec.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
