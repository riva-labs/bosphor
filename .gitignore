# env
.env
.env.*
!.env.example
!.env.*.example
relayer/.env.*
!relayer/.env.example

# contracts
contracts/evm/out/
contracts/evm/cache/
contracts/evm/broadcast/
contracts/evm/foundry.lock

# sui (under contracts/)
contracts/sui/executor/build/
contracts/sui/executor/deps/
contracts/sui/executor/Published.toml
contracts/sui/lz-receiver/build/
contracts/sui/lz-receiver/Published.toml
contracts/sui/lz-receiver/traces/

# dependencies
node_modules/
relayer/node_modules/
relayer/dist/

# website
website/node_modules/
website/build/
website/.docusaurus/

# confidential
AGREEMENT*.md

# internal grant / milestone reports (foundation-facing, kept local only)
MILESTONE_*_STATUS_REPORT.md
MILESTONE_*_STATUS_REPORT.docx

# relayer runtime state
relayer/sui-cursor.json
relayer/sui-sent-proofs.json
relayer/.sui-checkpoint-cursor

# logs
*.log

# tooling
.gstack/
.claude/
.cursor/
CLAUDE.md
codex.md

# misc
.DS_Store

# canary
canary/node_modules

# local hosting overlay (contains a Grafana admin password)
docker-compose.hosting.yml

# host-specific compose override (Docker auto-merges it; local topology only)
docker-compose.override.yml

# host-specific deployment stacks (testnet relayer + dapp index). The live
# containers reference these by path; they stay on the host, like the overlays
# above, and out of the public repo.
docker-compose.testnet-relayer.yml
docker-compose.dapp-index.yml
relayer-testnet-nginx.conf
dapp-index/

# cloudflared tunnel config (real tunnel UUID + host topology; see config.example.yml)
cloudflared/config.yml

# brand asset drop (full mascot set; website tracks the subset it uses)
bosphor_mascot/
